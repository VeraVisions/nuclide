name: Test on PR

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  build:
    runs-on: docker

    - uses: actions/checkout@v3
      with:
        fetch-depth: 1

    - name: make game
      run: make game GAME=base
